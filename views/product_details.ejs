<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("partials/head.ejs") %>
    <style>
      .custom-image {
        height: 30vh; 
        width: 100%; 
        object-fit: cover; 
      }
    </style>
    <title>Product Detail</title>
  </head>
  <body>
    <%- include("partials/navbar.ejs") %>
    
    <div class="container m-5">
      <h2><%= product.product_name %> Details</h2>
      <div class="row">
        <div class=" col-md-6 py-3 d-flex justify-content-center"
        >
          <img
            src="/img/product-sample.png"
            class="img-fluid h-50 "

            alt="Product Image"
          />
        </div>
        <div class="col-md-6">
          <h3><%= product.product_brand %> <%= product.product_name %> <%= product.storage_capacity %>GB <%= product.color %></h2>
          <p class="fs-3"><strong>$<%= product.price %></strong></p>
          <button class="btn btn-warning mt-3" type="button" data-bs-toggle="collapse" data-bs-target="#specifications" aria-expanded="false" aria-controls="specifications">Specifications</button>
          <div class="collapse mt-3" id="specifications">
            <p><strong>Weight:</strong> <%= product.weight %> grams</p>
            <p><strong>Dimensions:</strong> <%= product.dimensions %> inches</p>
            <p><strong>Operating System:</strong> <%= product.OS %></p>
            <p><strong>Screen Size:</strong> <%= product.screensize %></p>
            <p><strong>Resolution:</strong> <%= product.resolution %></p>
            <p><strong>Chipset:</strong> <%= product.chipset %></p>
            <p><strong>RAM:</strong> <%= product.ram %></p>
            <p><strong>Battery Capacity:</strong> <%= product.battery_capacity %></p>
            <p><strong>Rear Camera:</strong> <%= product.rear_camera %></p>
            <p><strong>Front Camera:</strong> <%= product.front_camera %></p>
          </div>
          <a href="/homepage" class="btn btn-secondary mt-3">Back to Homepage</a>
        </div>
      </div>
    </div>


    <%- include("partials/footer.ejs") %>

    <!-- partials for all scripts tag -->
    <%- include("partials/script.ejs") %>
  </body>
</html>
